-- Flattened FSM model generated from stdin
-- Dumped layers are: model Untime_monitors 

MODULE main
-- Input variables from layer 'model'
IVAR
tstartprocess.EVENT : {PROCESS_START, PROCESS_END, STUTTER};
twork.EVENT : {WORK_START, RUN, WORK_END, STUTTER};
tcooldown.EVENT : {COOL_START, COOL_END, STUTTER};
cmachine.EVENT : {TURN_ON, RUN, FINISH, COOLED, COOL, STUTTER};
__delta : real;

-- Input variables from layer 'Untime_monitors'
-- State variables from layer 'model'
VAR
global_clock : real;
twork.__CLOCK__c_2 : real;
cmachine.__CLOCK__c_1 : real;
tstartprocess.location : {OFF, ON};
tstartprocess.start : boolean;
tstartprocess.end : boolean;
twork.location : {OFF, READY, RUNNING};
twork.start : boolean;
twork.end : boolean;
tcooldown.location : {OFF, COOLING};
tcooldown.start : boolean;
tcooldown.end : boolean;
cmachine.location : {OFF, IDLE, RUNNING, COOLING};
time : real;
__iota : boolean;

-- State variables from layer 'Untime_monitors'
-- Frozen variables from layer 'model'
-- Frozen variables from layer 'Untime_monitors'
-- Functions from layer 'model'
-- Functions from layer 'Untime_monitors'
-- Defines from layer 'model'
DEFINE
__is_timed_trans := __expr0;

-- Defines from layer 'Untime_monitors'
CONSTANTS
 OFF, ON, PROCESS_START, PROCESS_END, STUTTER, READY, RUNNING, WORK_START, RUN, WORK_END, COOLING, COOL_START, COOL_END, IDLE, TURN_ON, FINISH, COOLED, COOL ;

-- Assignments from layer 'model'

-- Assignments from layer 'Untime_monitors'

INIT
 tstartprocess.location = OFF

INIT
 twork.location = OFF

INIT
 twork.__CLOCK__c_2 = 0

INIT
 tcooldown.location = OFF

INIT
 cmachine.location = OFF

INIT
 cmachine.__CLOCK__c_1 = 0

INIT
 global_clock = 0

INIT
 time = 0

INIT
 __iota


INVAR
 (cmachine.location = COOLING -> cmachine.__CLOCK__c_1 <= 10)

INVAR
 (twork.location = READY -> twork.__CLOCK__c_2 <= 5)


TRANS
 (!__expr0 | (__delta > 0 & (((((((((((((next(global_clock) = global_clock + __delta & next(twork.__CLOCK__c_2) = twork.__CLOCK__c_2 + __delta) & next(cmachine.__CLOCK__c_1) = cmachine.__CLOCK__c_1 + __delta) & next(tstartprocess.location) = tstartprocess.location) & next(tstartprocess.start) = tstartprocess.start) & next(tstartprocess.end) = tstartprocess.end) & __expr1) & next(twork.start) = twork.start) & next(twork.end) = twork.end) & __expr2) & next(tcooldown.start) = tcooldown.start) & next(tcooldown.end) = tcooldown.end) & __expr3) & next(time) = time + __delta)))

TRANS
 (__expr0 | (((((((((((tstartprocess.EVENT = STUTTER -> tstartprocess.location = next(tstartprocess.location)) & (tstartprocess.EVENT = PROCESS_START -> (tstartprocess.location = OFF & next(tstartprocess.location) = ON))) & (tstartprocess.EVENT = PROCESS_END -> (tstartprocess.location = ON & next(tstartprocess.location) = OFF))) & (tstartprocess.start <-> tstartprocess.EVENT = PROCESS_START)) & (tstartprocess.end <-> tstartprocess.EVENT = PROCESS_END)) & ((((((__expr4 -> ((twork.location = OFF & next(twork.location) = READY) & next(twork.__CLOCK__c_2) = 0)) & (twork.EVENT = RUN -> ((twork.location = READY & next(twork.location) = RUNNING) & __expr5))) & (__expr6 -> ((twork.location = RUNNING & next(twork.location) = OFF) & __expr5))) & (twork.EVENT = STUTTER -> (__expr1 & __expr5))) & (twork.start <-> __expr4)) & (twork.end <-> __expr6))) & (((((tcooldown.EVENT = COOL_START -> (tcooldown.location = OFF & next(tcooldown.location) = COOLING)) & (__expr7 -> (tcooldown.location = COOLING & next(tcooldown.location) = OFF))) & (tcooldown.EVENT = STUTTER -> __expr2)) & (tcooldown.start <-> tcooldown.EVENT = COOL_START)) & (tcooldown.end <-> __expr7))) & ((((((cmachine.EVENT = TURN_ON -> ((cmachine.location = OFF & next(cmachine.location) = IDLE) & __expr8)) & (cmachine.EVENT = RUN -> ((cmachine.location = IDLE & next(cmachine.location) = RUNNING) & __expr8))) & (cmachine.EVENT = FINISH -> ((cmachine.location = RUNNING & next(cmachine.location) = COOLING) & next(cmachine.__CLOCK__c_1) = 0))) & (cmachine.EVENT = COOLED -> ((__expr9 & cmachine.__CLOCK__c_1 >= 10) & __expr8))) & (cmachine.EVENT = COOL -> (__expr9 & __expr8))) & (cmachine.EVENT = STUTTER -> (__expr3 & __expr8)))) & ((((global_clock = next(global_clock) & (__expr4 <-> cmachine.EVENT = TURN_ON)) & (twork.EVENT = RUN <-> cmachine.EVENT = RUN)) & (__expr6 <-> cmachine.EVENT = FINISH)) & (__expr7 <-> cmachine.EVENT = COOL))) & __delta = 0) & (__iota & next(__iota))))

TRANS
 (!(!__iota | !next(__iota)) | __expr0)







-- Symbols introduced by the dumper:
DEFINE __expr2 := next(tcooldown.location) = tcooldown.location ;

DEFINE __expr8 := next(cmachine.__CLOCK__c_1) = cmachine.__CLOCK__c_1 ;

DEFINE __expr1 := next(twork.location) = twork.location ;

DEFINE __expr3 := next(cmachine.location) = cmachine.location ;

DEFINE __expr0 := time != next(time) ;

DEFINE __expr5 := next(twork.__CLOCK__c_2) = twork.__CLOCK__c_2 ;

DEFINE __expr9 := (cmachine.location = COOLING & next(cmachine.location) = OFF) ;

DEFINE __expr7 := tcooldown.EVENT = COOL_END ;

DEFINE __expr4 := twork.EVENT = WORK_START ;

DEFINE __expr6 := twork.EVENT = WORK_END ;

