-- Flattened FSM model generated from stdin
-- Dumped layers are: model Untime_monitors 

MODULE main
-- Input variables from layer 'model'
IVAR
tCHARGE.EVENT : {CHARGE_START, CHARGE_END, STUTTER};
tDSN.EVENT : {DSN_START, DSN_END, STUTTER};
tSBO.EVENT : {SBO_START, SBO_END, STUTTER};
tTCM.EVENT : {TCM_START, TCM_END, STUTTER};
__delta : real;

-- Input variables from layer 'Untime_monitors'
-- State variables from layer 'model'
VAR
global_clock : real;
tCHARGE.__CLOCK__c : real;
tDSN.__CLOCK__c : real;
tSBO.__CLOCK__c : real;
tTCM.__CLOCK__c : real;
tCHARGE.location : {OFF, ON};
tCHARGE.start : boolean;
tCHARGE.end : boolean;
tDSN.location : {OFF, ON};
tDSN.start : boolean;
tDSN.end : boolean;
tSBO.location : {OFF, ON};
tSBO.start : boolean;
tSBO.end : boolean;
tTCM.location : {OFF, ON};
tTCM.start : boolean;
tTCM.end : boolean;
soc : real;
time : real;
__iota : boolean;

-- State variables from layer 'Untime_monitors'
-- Frozen variables from layer 'model'
-- Frozen variables from layer 'Untime_monitors'
-- Functions from layer 'model'
-- Functions from layer 'Untime_monitors'
-- Defines from layer 'model'
DEFINE
__is_timed_trans := __expr0;

-- Defines from layer 'Untime_monitors'
CONSTANTS
 OFF, ON, CHARGE_START, CHARGE_END, STUTTER, DSN_START, DSN_END, SBO_START, SBO_END, TCM_START, TCM_END ;

-- Assignments from layer 'model'

-- Assignments from layer 'Untime_monitors'

INIT
 tCHARGE.location = OFF

INIT
 tCHARGE.__CLOCK__c = 0

INIT
 tDSN.location = OFF

INIT
 tDSN.__CLOCK__c = 0

INIT
 tSBO.location = OFF

INIT
 tSBO.__CLOCK__c = 0

INIT
 tTCM.location = OFF

INIT
 tTCM.__CLOCK__c = 0

INIT
 global_clock = 0

INIT
 soc = 200

INIT
 time = 0

INIT
 __iota



TRANS
 (!__expr0 | (__delta > 0 & ((((((((((((((((((next(global_clock) = global_clock + __delta & next(tCHARGE.__CLOCK__c) = tCHARGE.__CLOCK__c + __delta) & next(tDSN.__CLOCK__c) = tDSN.__CLOCK__c + __delta) & next(tSBO.__CLOCK__c) = tSBO.__CLOCK__c + __delta) & next(tTCM.__CLOCK__c) = tTCM.__CLOCK__c + __delta) & next(tCHARGE.location) = tCHARGE.location) & next(tCHARGE.start) = tCHARGE.start) & next(tCHARGE.end) = tCHARGE.end) & next(tDSN.location) = tDSN.location) & next(tDSN.start) = tDSN.start) & next(tDSN.end) = tDSN.end) & next(tSBO.location) = tSBO.location) & next(tSBO.start) = tSBO.start) & next(tSBO.end) = tSBO.end) & next(tTCM.location) = tTCM.location) & next(tTCM.start) = tTCM.start) & next(tTCM.end) = tTCM.end) & next(soc) = soc) & next(time) = time + __delta)))

TRANS
 (__expr0 | (((((((((((tCHARGE.EVENT = STUTTER -> (tCHARGE.location = next(tCHARGE.location) & __expr1)) & (__expr2 -> ((tCHARGE.location = OFF & next(tCHARGE.location) = ON) & next(tCHARGE.__CLOCK__c) = 0))) & (__expr3 -> ((tCHARGE.location = ON & next(tCHARGE.location) = OFF) & __expr1))) & (tCHARGE.start <-> __expr2)) & (tCHARGE.end <-> __expr3)) & (((((tDSN.EVENT = STUTTER -> (tDSN.location = next(tDSN.location) & __expr4)) & (__expr5 -> ((tDSN.location = OFF & next(tDSN.location) = ON) & next(tDSN.__CLOCK__c) = 0))) & (__expr6 -> ((tDSN.location = ON & next(tDSN.location) = OFF) & __expr4))) & (tDSN.start <-> __expr5)) & (tDSN.end <-> __expr6))) & (((((tSBO.EVENT = STUTTER -> (tSBO.location = next(tSBO.location) & __expr7)) & (__expr8 -> ((tSBO.location = OFF & next(tSBO.location) = ON) & next(tSBO.__CLOCK__c) = 0))) & (__expr9 -> ((tSBO.location = ON & next(tSBO.location) = OFF) & __expr7))) & (tSBO.start <-> __expr8)) & (tSBO.end <-> __expr9))) & (((((tTCM.EVENT = STUTTER -> (tTCM.location = next(tTCM.location) & __expr10)) & (__expr11 -> ((tTCM.location = OFF & next(tTCM.location) = ON) & next(tTCM.__CLOCK__c) = 0))) & (__expr12 -> ((tTCM.location = ON & next(tTCM.location) = OFF) & __expr10))) & (tTCM.start <-> __expr11)) & (tTCM.end <-> __expr12))) & ((((((((global_clock = next(global_clock) & (__expr2 -> soc >= 0)) & (__expr3 -> (__expr13 >= 4.0 * tCHARGE.__CLOCK__c & __expr13 <= 5.0 * tCHARGE.__CLOCK__c))) & (__expr5 -> soc >= 2.2 * tDSN.__CLOCK__c)) & (__expr6 -> (__expr14 >= 2.0 * tDSN.__CLOCK__c & __expr14 <= 2.2 * tDSN.__CLOCK__c))) & (__expr8 -> soc >= 0.2 * tSBO.__CLOCK__c)) & (__expr9 -> (__expr14 >= 0.1 * tSBO.__CLOCK__c & __expr14 <= 0.2 * tSBO.__CLOCK__c))) & (__expr11 -> soc >= 1.1 * tTCM.__CLOCK__c)) & (__expr12 -> (__expr14 >= 0.9 * tTCM.__CLOCK__c & __expr14 <= 1.1 * tTCM.__CLOCK__c)))) & __delta = 0) & (__iota & next(__iota))))

TRANS
 (!(!__iota | !next(__iota)) | __expr0)



INVARSPEC
 (!__iota | soc >= 40);






-- Symbols introduced by the dumper:
DEFINE __expr0 := time != next(time) ;

DEFINE __expr14 := soc - next(soc) ;

DEFINE __expr5 := tDSN.EVENT = DSN_START ;

DEFINE __expr4 := next(tDSN.__CLOCK__c) = tDSN.__CLOCK__c ;

DEFINE __expr2 := tCHARGE.EVENT = CHARGE_START ;

DEFINE __expr1 := next(tCHARGE.__CLOCK__c) = tCHARGE.__CLOCK__c ;

DEFINE __expr8 := tSBO.EVENT = SBO_START ;

DEFINE __expr6 := tDSN.EVENT = DSN_END ;

DEFINE __expr7 := next(tSBO.__CLOCK__c) = tSBO.__CLOCK__c ;

DEFINE __expr13 := next(soc) - soc ;

DEFINE __expr3 := tCHARGE.EVENT = CHARGE_END ;

DEFINE __expr11 := tTCM.EVENT = TCM_START ;

DEFINE __expr9 := tSBO.EVENT = SBO_END ;

DEFINE __expr10 := next(tTCM.__CLOCK__c) = tTCM.__CLOCK__c ;

DEFINE __expr12 := tTCM.EVENT = TCM_END ;

