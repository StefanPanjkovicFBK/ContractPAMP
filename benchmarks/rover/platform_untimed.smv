-- Flattened FSM model generated from stdin
-- Dumped layers are: model Untime_monitors 

MODULE main
-- Input variables from layer 'model'
IVAR
tmove.EVENT : {MOVE_START, MOVE_END, STUTTER};
tcomm.EVENT : {COMM_START, TURN_ON, RESUME, RESUMED, TX_START, TX_END, COMM_END, STUTTER};
cnav.EVENT : {NAV_START, NAV_END, STUTTER};
ctx.EVENT : {TURN_ON, TX_START, TX_END, SUSPEND, RESUME, STUTTER};
__delta : real;

-- Input variables from layer 'Untime_monitors'
-- State variables from layer 'model'
VAR
global_clock : real;
tcomm.__CLOCK__c_2 : real;
tcomm.__CLOCK__c_3 : real;
ctx.__CLOCK__c_4 : real;
ctx.__CLOCK__c_5 : real;
tmove.location : {OFF, MOVING};
tmove.start : boolean;
tmove.end : boolean;
tcomm.location : {OFF, READY, RESUMING, SENDING, SENT};
tcomm.start : boolean;
tcomm.end : boolean;
cnav.location : {OFF, MOVING};
ctx.location : {OFF, IDLE, TX, STANDBY};
time : real;
__iota : boolean;

-- State variables from layer 'Untime_monitors'
-- Frozen variables from layer 'model'
-- Frozen variables from layer 'Untime_monitors'
-- Functions from layer 'model'
-- Functions from layer 'Untime_monitors'
-- Defines from layer 'model'
DEFINE
__is_timed_trans := __expr13;

-- Defines from layer 'Untime_monitors'
CONSTANTS
 OFF, MOVING, MOVE_START, MOVE_END, STUTTER, READY, RESUMING, SENDING, SENT, COMM_START, TURN_ON, RESUME, RESUMED, TX_START, TX_END, COMM_END, NAV_START, NAV_END, IDLE, TX, STANDBY, SUSPEND ;

-- Assignments from layer 'model'

-- Assignments from layer 'Untime_monitors'

INIT
 tmove.location = OFF

INIT
 tcomm.location = OFF

INIT
 tcomm.__CLOCK__c_2 = 0

INIT
 tcomm.__CLOCK__c_3 = 0

INIT
 cnav.location = OFF

INIT
 ctx.location = OFF

INIT
 ctx.__CLOCK__c_4 = 0

INIT
 ctx.__CLOCK__c_5 = 0

INIT
 global_clock = 0

INIT
 time = 0

INIT
 __iota


INVAR
 (__expr14 -> ctx.__CLOCK__c_4 <= 30)

INVAR
 (tcomm.location = SENDING -> tcomm.__CLOCK__c_2 <= 15)

INVAR
 (tcomm.location = RESUMING -> tcomm.__CLOCK__c_3 <= 2)

INVAR
 (__expr15 -> tcomm.__CLOCK__c_2 <= 5)


TRANS
 (!__expr13 | (__delta > 0 & (((((((((((((next(global_clock) = global_clock + __delta & next(tcomm.__CLOCK__c_2) = tcomm.__CLOCK__c_2 + __delta) & next(tcomm.__CLOCK__c_3) = tcomm.__CLOCK__c_3 + __delta) & next(ctx.__CLOCK__c_4) = ctx.__CLOCK__c_4 + __delta) & next(ctx.__CLOCK__c_5) = ctx.__CLOCK__c_5 + __delta) & next(tmove.location) = tmove.location) & next(tmove.start) = tmove.start) & next(tmove.end) = tmove.end) & next(tcomm.location) = tcomm.location) & next(tcomm.start) = tcomm.start) & next(tcomm.end) = tcomm.end) & next(cnav.location) = cnav.location) & __expr16) & next(time) = time + __delta)))

TRANS
 (__expr13 | (((((((((((tmove.EVENT = STUTTER -> tmove.location = next(tmove.location)) & (__expr17 -> (tmove.location = OFF & next(tmove.location) = MOVING))) & (__expr18 -> (tmove.location = MOVING & next(tmove.location) = OFF))) & (tmove.start <-> __expr17)) & (tmove.end <-> __expr18)) & ((((((((((tcomm.EVENT = COMM_START -> (((tcomm.location = OFF & __expr19) & next(tcomm.__CLOCK__c_2) = 0) & __expr20)) & (tcomm.EVENT = TURN_ON -> (((__expr15 & __expr19) & __expr21) & __expr20))) & (tcomm.EVENT = RESUME -> (((__expr15 & next(tcomm.location) = RESUMING) & __expr21) & next(tcomm.__CLOCK__c_3) = 0))) & (tcomm.EVENT = RESUMED -> (((tcomm.location = RESUMING & __expr19) & __expr21) & __expr20))) & (tcomm.EVENT = TX_START -> (((__expr15 & next(tcomm.location) = SENDING) & __expr21) & __expr20))) & (tcomm.EVENT = TX_END -> (((tcomm.location = SENDING & next(tcomm.location) = SENT) & __expr21) & __expr20))) & (tcomm.EVENT = COMM_END -> (((tcomm.location = SENT & next(tcomm.location) = OFF) & __expr21) & __expr20))) & (tcomm.EVENT = STUTTER -> ((tcomm.location = next(tcomm.location) & tcomm.__CLOCK__c_2 = next(tcomm.__CLOCK__c_2)) & tcomm.__CLOCK__c_3 = next(tcomm.__CLOCK__c_3)))) & (tcomm.start <-> tcomm.EVENT = COMM_START)) & (tcomm.end <-> tcomm.EVENT = COMM_END))) & (((cnav.EVENT = STUTTER -> cnav.location = next(cnav.location)) & (cnav.EVENT = NAV_START -> (cnav.location = OFF & next(cnav.location) = MOVING))) & (cnav.EVENT = NAV_END -> (cnav.location = MOVING & next(cnav.location) = OFF)))) & ((((((ctx.EVENT = TURN_ON -> (((ctx.location = OFF & __expr22) & __expr23) & __expr24)) & (ctx.EVENT = TX_START -> (((__expr14 & next(ctx.location) = TX) & __expr25) & next(ctx.__CLOCK__c_5) = 0))) & (ctx.EVENT = TX_END -> ((((ctx.location = TX & __expr22) & __expr23) & ctx.__CLOCK__c_5 >= 5) & __expr24))) & (ctx.EVENT = SUSPEND -> ((((__expr14 & next(ctx.location) = STANDBY) & ctx.__CLOCK__c_4 >= 30) & __expr25) & __expr24))) & (ctx.EVENT = RESUME -> (((ctx.location = STANDBY & __expr22) & __expr23) & __expr24))) & (ctx.EVENT = STUTTER -> ((__expr16 & __expr25) & __expr24)))) & ((((((global_clock = next(global_clock) & (__expr17 <-> cnav.EVENT = NAV_START)) & (__expr18 <-> cnav.EVENT = NAV_END)) & (tcomm.EVENT = TURN_ON <-> ctx.EVENT = TURN_ON)) & (tcomm.EVENT = RESUME <-> ctx.EVENT = RESUME)) & (tcomm.EVENT = TX_START <-> ctx.EVENT = TX_START)) & (tcomm.EVENT = TX_END <-> ctx.EVENT = TX_END))) & __delta = 0) & (__iota & next(__iota))))

TRANS
 (!(!__iota | !next(__iota)) | __expr13)



INVARSPEC
 (!__iota | !(tcomm.location = RESUMING));






-- Symbols introduced by the dumper:
DEFINE __expr21 := next(tcomm.__CLOCK__c_2) = tcomm.__CLOCK__c_2 ;

DEFINE __expr20 := next(tcomm.__CLOCK__c_3) = tcomm.__CLOCK__c_3 ;

DEFINE __expr16 := next(ctx.location) = ctx.location ;

DEFINE __expr24 := next(ctx.__CLOCK__c_5) = ctx.__CLOCK__c_5 ;

DEFINE __expr19 := next(tcomm.location) = READY ;

DEFINE __expr23 := next(ctx.__CLOCK__c_4) = 0 ;

DEFINE __expr22 := next(ctx.location) = IDLE ;

DEFINE __expr15 := tcomm.location = READY ;

DEFINE __expr25 := next(ctx.__CLOCK__c_4) = ctx.__CLOCK__c_4 ;

DEFINE __expr18 := tmove.EVENT = MOVE_END ;

DEFINE __expr14 := ctx.location = IDLE ;

DEFINE __expr13 := time != next(time) ;

DEFINE __expr17 := tmove.EVENT = MOVE_START ;

