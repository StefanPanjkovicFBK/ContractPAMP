instance Location L1, L2, L3;

distance_lb(L1, L1) := 0;
distance_lb(L1, L2) := 10;
distance_lb(L1, L3) := 20;
distance_lb(L2, L1) := 10;
distance_lb(L2, L2) := 0;
distance_lb(L2, L3) := 10;
distance_lb(L3, L1) := 20;
distance_lb(L3, L2) := 10;
distance_lb(L3, L3) := 0;

distance_ub(L1, L1) := 0;
distance_ub(L1, L2) := 110;
distance_ub(L1, L3) := 220;
distance_ub(L2, L1) := 110;
distance_ub(L2, L2) := 0;
distance_ub(L2, L3) := 110;
distance_ub(L3, L1) := 220;
distance_ub(L3, L2) := 110;
distance_ub(L3, L3) := 0;

id(L1) := 1;
id(L2) := 2;
id(L3) := 3;

[start] truck_at(L1) := true;
[start] truck_at(L2) := false;
[start] truck_at(L3) := false;

[start] loc_packages(L1) := 6;
[start] loc_packages(L2) := 0;
[start] loc_packages(L3) := 0;
[start] truck_packages := 0;

[start] loading := false;
[start] loaded := false;

[end] loc_packages(L2) == 3;
[end] loc_packages(L3) == 3;
