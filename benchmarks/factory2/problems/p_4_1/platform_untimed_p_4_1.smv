-- Flattened FSM model generated from stdin
-- Dumped layers are: model Untime_monitors 

MODULE main
-- Input variables from layer 'model'
IVAR
twork.EVENT : {WORK_START, RUN, WORK_END, STUTTER};
tcooldown.EVENT : {COOL_START, COOL_END, STUTTER};
cmachine.EVENT : {TURN_ON, RUN, FINISH, COOLED, COOL, STUTTER};
ccontroller.EVENT : {WORK_DONE, SWITCH_OFF, STUTTER};
__delta : real;

-- Input variables from layer 'Untime_monitors'
-- State variables from layer 'model'
VAR
global_clock : real;
twork.__CLOCK__c_2 : real;
cmachine.__CLOCK__c_1 : real;
ccontroller.__CLOCK__c_3 : real;
twork.location : {OFF, READY, RUNNING};
twork.start : boolean;
twork.end : boolean;
tcooldown.location : {OFF, COOLING};
tcooldown.start : boolean;
tcooldown.end : boolean;
cmachine.location : {OFF, IDLE, RUNNING, COOLING};
ccontroller.location : {ON, OFF};
time : real;
__iota : boolean;

-- State variables from layer 'Untime_monitors'
-- Frozen variables from layer 'model'
-- Frozen variables from layer 'Untime_monitors'
-- Functions from layer 'model'
-- Functions from layer 'Untime_monitors'
-- Defines from layer 'model'
DEFINE
__is_timed_trans := __expr0;

-- Defines from layer 'Untime_monitors'
CONSTANTS
 OFF, READY, RUNNING, WORK_START, RUN, WORK_END, STUTTER, COOLING, COOL_START, COOL_END, IDLE, TURN_ON, FINISH, COOLED, COOL, ON, WORK_DONE, SWITCH_OFF ;

-- Assignments from layer 'model'

-- Assignments from layer 'Untime_monitors'

INIT
 twork.location = OFF

INIT
 twork.__CLOCK__c_2 = 0

INIT
 tcooldown.location = OFF

INIT
 cmachine.location = OFF

INIT
 cmachine.__CLOCK__c_1 = 0

INIT
 __expr1

INIT
 ccontroller.__CLOCK__c_3 = 0

INIT
 global_clock = 0

INIT
 time = 0

INIT
 __iota


INVAR
 (__expr1 -> ccontroller.__CLOCK__c_3 <= 103)

INVAR
 (cmachine.location = COOLING -> cmachine.__CLOCK__c_1 <= 10)

INVAR
 (twork.location = READY -> twork.__CLOCK__c_2 <= 5)


TRANS
 (!__expr0 | (__delta > 0 & ((((((((((((next(global_clock) = global_clock + __delta & next(twork.__CLOCK__c_2) = twork.__CLOCK__c_2 + __delta) & next(cmachine.__CLOCK__c_1) = cmachine.__CLOCK__c_1 + __delta) & next(ccontroller.__CLOCK__c_3) = ccontroller.__CLOCK__c_3 + __delta) & __expr2) & next(twork.start) = twork.start) & next(twork.end) = twork.end) & __expr3) & next(tcooldown.start) = tcooldown.start) & next(tcooldown.end) = tcooldown.end) & __expr4) & __expr5) & next(time) = time + __delta)))

TRANS
 (__expr0 | ((((((((((((__expr6 -> ((twork.location = OFF & next(twork.location) = READY) & next(twork.__CLOCK__c_2) = 0)) & (twork.EVENT = RUN -> ((twork.location = READY & next(twork.location) = RUNNING) & __expr7))) & (__expr8 -> ((twork.location = RUNNING & next(twork.location) = OFF) & __expr7))) & (twork.EVENT = STUTTER -> (__expr2 & __expr7))) & (twork.start <-> __expr6)) & (twork.end <-> __expr8)) & (((((tcooldown.EVENT = COOL_START -> (tcooldown.location = OFF & next(tcooldown.location) = COOLING)) & (__expr9 -> (tcooldown.location = COOLING & next(tcooldown.location) = OFF))) & (tcooldown.EVENT = STUTTER -> __expr3)) & (tcooldown.start <-> tcooldown.EVENT = COOL_START)) & (tcooldown.end <-> __expr9))) & ((((((cmachine.EVENT = TURN_ON -> ((cmachine.location = OFF & next(cmachine.location) = IDLE) & __expr10)) & (cmachine.EVENT = RUN -> ((cmachine.location = IDLE & next(cmachine.location) = RUNNING) & __expr10))) & (cmachine.EVENT = FINISH -> ((cmachine.location = RUNNING & next(cmachine.location) = COOLING) & next(cmachine.__CLOCK__c_1) = 0))) & (cmachine.EVENT = COOLED -> ((__expr11 & cmachine.__CLOCK__c_1 >= 10) & __expr10))) & (cmachine.EVENT = COOL -> (__expr11 & __expr10))) & (cmachine.EVENT = STUTTER -> (__expr4 & __expr10)))) & (((ccontroller.EVENT = WORK_DONE -> ((__expr1 & next(ccontroller.location) = ON) & __expr12)) & (ccontroller.EVENT = SWITCH_OFF -> (((__expr1 & next(ccontroller.location) = OFF) & ccontroller.__CLOCK__c_3 >= 103) & __expr12))) & (ccontroller.EVENT = STUTTER -> (__expr5 & __expr12)))) & (((((global_clock = next(global_clock) & (__expr6 <-> cmachine.EVENT = TURN_ON)) & (twork.EVENT = RUN <-> cmachine.EVENT = RUN)) & (__expr8 <-> cmachine.EVENT = FINISH)) & (__expr8 <-> ccontroller.EVENT = WORK_DONE)) & (__expr9 <-> cmachine.EVENT = COOL))) & __delta = 0) & (__iota & next(__iota))))

TRANS
 (!(!__iota | !next(__iota)) | __expr0)







-- Symbols introduced by the dumper:
DEFINE __expr4 := next(cmachine.location) = cmachine.location ;

DEFINE __expr12 := next(ccontroller.__CLOCK__c_3) = ccontroller.__CLOCK__c_3 ;

DEFINE __expr2 := next(twork.location) = twork.location ;

DEFINE __expr11 := (cmachine.location = COOLING & next(cmachine.location) = OFF) ;

DEFINE __expr5 := next(ccontroller.location) = ccontroller.location ;

DEFINE __expr1 := ccontroller.location = ON ;

DEFINE __expr9 := tcooldown.EVENT = COOL_END ;

DEFINE __expr7 := next(twork.__CLOCK__c_2) = twork.__CLOCK__c_2 ;

DEFINE __expr0 := time != next(time) ;

DEFINE __expr3 := next(tcooldown.location) = tcooldown.location ;

DEFINE __expr10 := next(cmachine.__CLOCK__c_1) = cmachine.__CLOCK__c_1 ;

DEFINE __expr6 := twork.EVENT = WORK_START ;

DEFINE __expr8 := twork.EVENT = WORK_END ;

