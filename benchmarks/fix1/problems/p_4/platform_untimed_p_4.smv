-- Flattened FSM model generated from stdin
-- Dumped layers are: model Untime_monitors 

MODULE main
-- Input variables from layer 'model'
IVAR
tswitch.EVENT : {SWITCH_START, SWITCH_END, STUTTER};
tfix.EVENT : {FIX_START, FIX_END, STUTTER};
ccontroller.EVENT : {FIX, SWITCH, STUTTER};
__delta : real;

-- Input variables from layer 'Untime_monitors'
-- State variables from layer 'model'
VAR
global_clock : real;
tswitch.location : {OFF, SWITCHING};
tswitch.start : boolean;
tswitch.end : boolean;
tfix.location : {OFF, FIXING};
tfix.start : boolean;
tfix.end : boolean;
ccontroller.location : {BROKEN, FIXED};
ccontroller.x : real;
switch_running_ : boolean;
fix_running_ : boolean;
time : real;
__iota : boolean;

-- State variables from layer 'Untime_monitors'
-- Frozen variables from layer 'model'
-- Frozen variables from layer 'Untime_monitors'
-- Functions from layer 'model'
-- Functions from layer 'Untime_monitors'
-- Defines from layer 'model'
DEFINE
__is_timed_trans := __expr0;

-- Defines from layer 'Untime_monitors'
CONSTANTS
 OFF, SWITCHING, SWITCH_START, SWITCH_END, STUTTER, FIXING, FIX_START, FIX_END, BROKEN, FIXED, FIX, SWITCH ;

-- Assignments from layer 'model'

-- Assignments from layer 'Untime_monitors'

INIT
 __expr1

INIT
 __expr2

INIT
 __expr3

INIT
 ccontroller.x = 4

INIT
 global_clock = 0

INIT
 !switch_running_

INIT
 !fix_running_

INIT
 time = 0

INIT
 __iota


INVAR
 (!fix_running_ -> __expr2)

INVAR
 (!switch_running_ -> __expr1)

INVAR
 (__expr4 -> ccontroller.x <= 3)


TRANS
 (!__expr0 | (__delta > 0 & (((((((((((next(global_clock) = global_clock + __delta & next(tswitch.location) = tswitch.location) & next(tswitch.start) = tswitch.start) & next(tswitch.end) = tswitch.end) & next(tfix.location) = tfix.location) & next(tfix.start) = tfix.start) & next(tfix.end) = tfix.end) & next(ccontroller.location) = ccontroller.location) & next(ccontroller.x) = ccontroller.x) & next(switch_running_) = switch_running_) & next(fix_running_) = fix_running_) & next(time) = time + __delta)))

TRANS
 (__expr0 | ((((((((((tswitch.EVENT = STUTTER -> tswitch.location = next(tswitch.location)) & (__expr5 -> (__expr1 & next(tswitch.location) = SWITCHING))) & (__expr6 -> (tswitch.location = SWITCHING & next(tswitch.location) = OFF))) & (tswitch.start <-> __expr5)) & (tswitch.end <-> __expr6)) & (((((tfix.EVENT = STUTTER -> tfix.location = next(tfix.location)) & (__expr7 -> (__expr2 & next(tfix.location) = FIXING))) & (__expr8 -> (tfix.location = FIXING & next(tfix.location) = OFF))) & (tfix.start <-> __expr7)) & (tfix.end <-> __expr8))) & (((ccontroller.EVENT = STUTTER -> (ccontroller.location = next(ccontroller.location) & ccontroller.x = next(ccontroller.x))) & (ccontroller.EVENT = FIX -> ((((__expr4 & __expr9) & __expr10) | ((__expr4 & __expr11) & __expr10)) | ((__expr3 & __expr11) & __expr10)))) & (ccontroller.EVENT = SWITCH -> ((__expr3 & __expr9) & next(ccontroller.x) = 0)))) & ((((((global_clock = next(global_clock) & (__expr5 -> next(switch_running_))) & (__expr6 -> !next(switch_running_))) & (__expr7 -> next(fix_running_))) & (__expr8 -> !next(fix_running_))) & (__expr6 <-> ccontroller.EVENT = SWITCH)) & (__expr8 <-> ccontroller.EVENT = FIX))) & __delta = 0) & (__iota & next(__iota))))

TRANS
 (!(!__iota | !next(__iota)) | __expr0)







-- Symbols introduced by the dumper:
DEFINE __expr11 := next(ccontroller.location) = FIXED ;

DEFINE __expr4 := ccontroller.location = BROKEN ;

DEFINE __expr8 := tfix.EVENT = FIX_END ;

DEFINE __expr6 := tswitch.EVENT = SWITCH_END ;

DEFINE __expr3 := ccontroller.location = FIXED ;

DEFINE __expr1 := tswitch.location = OFF ;

DEFINE __expr10 := next(ccontroller.x) = ccontroller.x + 1 ;

DEFINE __expr0 := time != next(time) ;

DEFINE __expr9 := next(ccontroller.location) = BROKEN ;

DEFINE __expr7 := tfix.EVENT = FIX_START ;

DEFINE __expr5 := tswitch.EVENT = SWITCH_START ;

DEFINE __expr2 := tfix.location = OFF ;

